[project]
name = "dutch-ml"
version = "0.1.0"
description = "Pipeline ML Dutch'78 — prédiction de victoire (finalRank == 1) à partir d'un état de partie en cours"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pandas>=2.2",
    "scikit-learn>=1.5",
    "matplotlib>=3.9",
    "xgboost>=2.1",
    "joblib>=1.4",
    "pyarrow>=17.0",
]

[tool.uv]
package = false
